[project]
name = "pdf-to-djvu-backend"
version = "0.1.0"
description = "FastAPI backend for PDF → DjVu conversion"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py311"
